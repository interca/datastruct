#include <iostream>
#include<algorithm>
#include<cstring>
using namespace std;
#include<vector>
#include<cmath>

void solve(){
   int n;
   cin>>t;
   vector<int>a(n);
   vector<int>b(n);
   vector<int>c(n);
   for(int i = 0 ;i< n ; i ++)cin>>a[i];
   for(int i = 0 ;i< n ; i ++)cin>>b[i];
   for(int i = 0 ;i< n ; i ++)cin>>c[i];
   
}

int main(){
   int t;
   cin>>t;
   while(t --)solve;
}

