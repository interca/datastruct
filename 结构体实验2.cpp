#include<stdio.h>
struct student {
	int number;
	 char name[10];
	int phone;
};
int main(){
	int a=1;
	printf("%\)
	struct student p;
	void input(struct student *p);
	input(&p);
	printf("%s %d %d",p.name.p.phone,p.number);
	return 0;
} 
void input(struct student *p){
	scanf("%s%d%d",&p->name,&p->number,&p->phone);
}

